# Chapter Drafts

Author drafts for the book. Only `12-markdown-demo.md` is read by the build. Its two marker-delimited halves are rendered by `src/pages/14-markdown-automation/01-markdown-automation.tsx` and `03-supported-elements.tsx`.
Editing the other files does not change the PDF – the book itself is hand-authored TSX in `src/pages/`.
